#include <stdio.h>
#include <stdlib.h>

/**
 * main - returns a combinatoin of number
 * Return: return 0 if successful
 */

int main(void)
{
	int i, j;

	for (j = 0; j < 100; j++)
	{
		if (i < j)
		{
			putchar((i / 10) + 48);
			putchar((i % 10) + 48);
			putchar (' ');
			putchar((j / 10) + 48);
			putchar((j % 10) + 48);
			if (i != 98 || j != 98)
			{
				putchar(',");
				putchar(' ');
			)
		}
	)
}

putchar('\n")

return (0):
}
